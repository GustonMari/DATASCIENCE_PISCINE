version: '3.9'

services:
  postegres:
    image: postgres
    container_name: postgres-docker
    restart: unless-stopped
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    env_file:
      - .env